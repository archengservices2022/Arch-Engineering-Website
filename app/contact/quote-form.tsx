"use client";
import { useState } from "react";
export default function QuoteForm(){const [sent,setSent]=useState(false);function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const subject=encodeURIComponent(`Project enquiry: ${f.get("service")}`);const body=encodeURIComponent(`Name: ${f.get("name")}\nEmail: ${f.get("email")}\nPhone: ${f.get("phone")}\nCompany: ${f.get("company")}\nService: ${f.get("service")}\nTimeline: ${f.get("timeline")}\nBudget: ${f.get("budget")}\n\nProject details:\n${f.get("details")}`);setSent(true);window.location.href=`mailto:info@archengineeringservices.com?subject=${subject}&body=${body}`;}return <form className="quote-form" onSubmit={submit}><div className="form-row"><label>Full name *<input name="name" required/></label><label>Email *<input name="email" type="email" required/></label></div><div className="form-row"><label>Phone<input name="phone" type="tel"/></label><label>Company<input name="company"/></label></div><label>Service needed *<select name="service" required defaultValue=""><option value="" disabled>Select a service</option><option>Mechanical CAD & drawings</option><option>Inventor iLogic / CAD automation</option><option>Custom software development</option><option>Web or mobile application</option><option>Not sure yet</option></select></label><div className="form-row"><label>Desired timeline<select name="timeline"><option>Flexible</option><option>Within 1 month</option><option>1–3 months</option><option>3+ months</option></select></label><label>Approximate budget<select name="budget"><option>Not decided</option><option>Under $5,000</option><option>$5,000–$15,000</option><option>$15,000+</option></select></label></div><label>Project details *<textarea name="details" rows={7} required placeholder="Describe the current problem, available files or systems, and the result you need."/></label><button className="button primary" type="submit">Prepare email enquiry →</button><small>This securely opens your email application with the project details filled in. You review and send the email.</small>{sent&&<p className="form-note">Your email application should now be open. If it did not open, email us directly using the address shown here.</p>}</form>}
